# Core dependencies
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0  # Python 3.9+ minimum - NumPy 2.0+ available
plotly>=5.17.0
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0
pytest>=7.4.0
pytest-cov>=4.1.0
requests>=2.31.0
authlib>=1.2.0  # OAuth 2.0 / OpenID Connect support
opentelemetry-api>=1.21.0  # Distributed tracing
opentelemetry-sdk>=1.21.0
opentelemetry-exporter-otlp>=1.21.0
yfinance>=0.2.0
shap>=0.42.0
tqdm>=4.66.0

# Performance & JIT compilation
numba>=0.58.0

# Financial & quantitative libraries
pandas-market-calendars>=4.1.0,<4.2.0  # 4.2.1+ may use Python 3.10+ syntax (| union), pin to 4.1.x for Python 3.9 compatibility
arch>=6.2.0
statsmodels>=0.14.0
Riskfolio-Lib>=5.0.0
ffn>=0.3.7

# Advanced optimization
cvxpy>=1.4.0
PyPortfolioOpt>=1.5.0
pulp>=2.7.0

# Machine learning enhancements
xgboost>=2.0.0
lightgbm>=4.1.0
catboost>=1.2.0
optuna>=3.4.0
mlflow>=2.8.0
schedule>=1.2.0  # For automated retraining

# Database & data persistence
sqlalchemy>=2.0.0
alembic>=1.12.0
redis>=5.0.0

# Parallel processing
dask>=2024.1.0
dask[dataframe]>=2024.1.0

# Testing & quality
pytest-benchmark>=4.0.0
pytest-xdist>=3.3.0
hypothesis>=6.92.0
mypy>=1.7.0

# Code quality
black>=26.1.0
isort>=5.12.0
pylint>=3.0.0
pre-commit>=3.5.0

# Logging & monitoring
structlog>=24.1.0
loguru>=0.7.2
sentry-sdk>=1.38.0
prometheus-client>=0.19.0  # For metrics collection

# Data validation
pydantic>=2.5.0  # Python 3.9+ minimum - Pydantic 2.11+ available
marshmallow>=3.20.0

# API & web (optional extensions)
fastapi>=0.104.0
uvicorn>=0.24.0

# Visualization enhancements
# mplfinance>=0.12.0  # Optional - not used in current codebase, 0.12.0 stable not available
seaborn>=0.13.0

# Additional data sources
pandas-datareader>=0.10.0

# Security & authentication
cryptography>=41.0.0  # For secrets encryption
bcrypt>=4.0.0  # For secure password hashing (industry standard)
pyotp>=2.9.0  # For TOTP-based MFA (Time-based One-Time Password)
qrcode>=7.4.0  # For MFA QR code generation
safety>=2.3.0  # For dependency vulnerability scanning
pip-audit>=2.6.0  # For dependency vulnerability scanning
tenacity>=8.2.0  # For retry logic with exponential backoff
circuitbreaker>=1.4.0  # For circuit breaker pattern

# Optional: Cloud secrets management
# boto3>=1.28.0  # For AWS Secrets Manager (uncomment if using AWS)
# hvac>=1.1.0  # For HashiCorp Vault (uncomment if using Vault)